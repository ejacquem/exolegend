#include <vector>
#include <queue>
#include <map>
#include <unordered_set>
#include <algorithm>
#include <set>
#include <gladiator.h>

class BFS
{
    public:
        static std::vector<MazeSquare*> shortestPath(MazeSquare* start, MazeSquare* target);
};